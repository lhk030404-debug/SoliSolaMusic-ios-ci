const ROUTE_PREFIX = import.meta.env.BASE_URL

export const ID_ROUTE = `${ROUTE_PREFIX}:type`
export const HASH_ID_ROUTE = `${ROUTE_PREFIX}:type/:hashId`
export const PERMALINK_ROUTE = `${ROUTE_PREFIX}:type/:handle/:slug`
