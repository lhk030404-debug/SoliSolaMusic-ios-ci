# Get an API key at audius.co/settings → Developer Apps.
# Register redirect URI: updateprofile://oauth/callback
# No server required — profile updates use OAuth from the app.
EXPO_PUBLIC_AUDIUS_API_KEY=your_api_key
